import React from 'react';
import Extraction from './Extraction';

const Exclude = () => {
    return (
        <div>
            <Extraction></Extraction>
            <p className='text-9xl'>Exclude</p>
        </div>
    );
};

export default Exclude;